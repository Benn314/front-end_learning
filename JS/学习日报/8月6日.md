```html
8月6日 【Ben】

遇到的问题
【问题】
1.vscode快速关闭打开的所有预览编辑器界面？

【解决】
1.上方界面条右击有全部关闭...

今日小结
1.学习了【JS】数组方法、内建对象的方法(slice、splice、call、apply)、浏览器的隐藏传递属性argument(另一个是this)、
forEach遍历、Data对象、Math工具类、基本数据类型(String、Boolean、Number)的包装类
2.【JS】P71看到了P83
3.【心得】浏览器与JS的交互并不简单，运行时调用了许多隐形的方法和属性，在没有了解之前是不知道平常语句底层是这么运行的。
像基本数据类型之所以能使用其对象的属性和方法，是因为浏览器会临时使用包装类(String、Boolean、Number)将其转换为对象，
然后在调用对象的属性和方法，调用完以后在将其转换为基本数据类型。很不错的想法！

明日计划
1.【JS】学到P90
```

